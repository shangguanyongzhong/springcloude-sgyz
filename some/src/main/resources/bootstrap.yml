spring:
  application:
    name: some
eureka:
  instance:
    non-secure-port: ${server.port:8085}
  client:
    service-url:
      defaultZone: http://${eureka.host:localhost}:${eureka.port:8761}/eureka/